# Todo Utils

This is a Python library for managing to-do lists.

## For End Users

### Installation

Clone the repository.

```bash
git clone https://github.com/zifeng-yang-tj/todo-utils.git
cd todo-utils
```

Install the package requirements in your Python environment (e.g., conda).

```bash
pip install -r requirements-test.txt
```
